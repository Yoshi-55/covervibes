<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "CoverVibes" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- OGP & Twitter Card -->
  <meta property="og:title" content="CoverVibes" />
  <meta property="og:description" content="CDショップのような試聴体験、新しい音楽と出会おう。" />
  <meta property="og:image" content="https://your-domain.example.com/covervibes.png" />
  <meta property="og:url" content="https://your-domain.example.com/" />
  <meta property="og:type" content="website" />
  <meta name="twitter:card" content="summary_large_image" />


  <%= yield :head %>
  <!-- Alpine.js CDN -->
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/CV.png" type="image/png" sizes="32x32">
    <link rel="icon" href="/CV.png" type="image/png">
    <link rel="apple-touch-icon" href="/cover.png" sizes="500x500">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body x-data="{ sidebarOpen: false }">
    <div class="relative">
      <div class="fixed top-0 left-0 w-full z-50">
        <div class="flex items-center">
          <!-- モバイル用 -->
          <button class="md:hidden btn btn-ghost btn-square" @click="sidebarOpen = true">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" /></svg>
          </button>
          <div class="flex-1">
            <%= render 'layouts/header' %>
          </div>
        </div>
      </div>
      <!-- モバイル用サイドバー -->
      <div class="md:hidden fixed inset-0 z-50" x-show="sidebarOpen" style="display: none;">
        <div class="fixed inset-0 bg-black bg-opacity-40" @click="sidebarOpen = false"></div>
        <div class="fixed top-0 left-0 h-full w-64 bg-base-200 shadow-lg z-50 p-0">
          <button class="btn btn-ghost btn-square absolute top-2 right-2" @click="sidebarOpen = false">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" /></svg>
          </button>
          <%= render 'layouts/sidebar' %>
        </div>
      </div>
      <div class="flex min-h-screen">
        <div class="hidden md:block fixed top-0 left-0 h-screen z-40" style="padding-top: 64px; width: 240px;">
          <%= render 'layouts/sidebar' %>
        </div>
        <main class="flex-1 pt-4 pr-6 pb-6 pl-6 md:pl-[240px]" style="padding-top: 64px;">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
